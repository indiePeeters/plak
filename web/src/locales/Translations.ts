export interface Translations {
  features: {
    homeHeader: {
      title: string,
      description: string,
      primaryAction: string,
      secondaryAction: string,
    },
    aboutUs: {
      title: string
      text: string
    }
  }
}
